{
  "name": "dashers-family-server",
  "type": "module",
  "private": true,
  "description": "Local-network-only sync server for Dashers Finance Manager. No external dependencies — Node built-ins only.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  }
}
